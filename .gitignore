# Build artifacts
build/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
Package.resolved

# Code coverage
*.gcno
*.gcda

# Implementation plans (the untracked working notes at the repo root only —
# docs/plans/ is tracked, and an unanchored "plans/" matched that too)
/plans/

# Local-only integration tests (require a real MPD server — must not be committed)
mikMPDTests/Local/

# One-off diagnostic dumps generated against a live server
untagged-files.txt
